- General syntax of `ip` commands (from `man ip`):
- ```
  ip <thing> <command>
  
  # Examples of things: address, link, route
  
  # Commands are thing-dependent, but some generic examples are: show, add, set, etc
  ```
