It turns out the "Copy Path" and "Copy Relative Path" keyboard shortcuts, as defined by default, don't work when the editor is focused. You need to first focus the editor on the desired file, then move the focus somewhere else (such as the terminal), and then input the keyboard shortcut.

That's dumb, I'm gonna change that. Also, the keyboard shortcuts themselves are difficult to input, so I'll probably change that too.

Never mind, there's an alternate input for them when the editor is focused: you input Cmd-K first.

### Keyboard Shortcuts I'm Memorizing:
- Shift-Cmd-\  - move focus to terminal tabs view
- Cmd-<d-arrow>  - move focus to terminal
