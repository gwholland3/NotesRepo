For now, specifically keyboard inputs

Helpful Tools:
- showkey (native to Linux, has macOS version)
- Key Codes (native to macOS)
- Run `cat` from a shell with no args, then start inputting
- Enter `ctrl-v` in Bash, which triggers a quoted insert. Then type any other key
